\documentclass[12pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage[T2A]{fontenc}
\usepackage[russian,english]{babel}
\usepackage{amsmath,amssymb}
\usepackage{geometry}
\usepackage{enumitem}
\usepackage{needspace}
\geometry{left=2.5cm, right=2.5cm, top=2.5cm, bottom=2.5cm}

\begin{document}

%% ══════════════════════════════════════════════════════════════
%%  VARIANTS  (one page block per variant)
%% ══════════════════════════════════════════════════════════════

{% for var in variants %}
\begin{center}
    {\Large\bfseries {{ title }}} \\[4pt]
    {\large Вариант~№\,{{ var.variant_num }}}
\end{center}
\vspace{0.8cm}

\begin{enumerate}[leftmargin=*, label=\textbf{\arabic*.}]
{% for task in var.tasks %}
    \needspace{4\baselineskip}
    \item {{ task.title }}
    \[
        {{ task.condition_latex }}
    \]
    \vspace{1.2cm}
{% endfor %}
\end{enumerate}

{% if not loop.last %}
\newpage
{% endif %}
{% endfor %}

%% ══════════════════════════════════════════════════════════════
%%  SOLUTIONS APPENDIX  (teacher copy — starts on a new page)
%% ══════════════════════════════════════════════════════════════

\newpage

\begin{center}
    {\Large\bfseries Приложение: Ответы и решения} \\[2pt]
    {\small\itshape (Appendix: Answers \& Step-by-Step Solutions)} \\[6pt]
    {\normalsize\bfseries {{ title }}}
\end{center}

\bigskip

{% for sol in solutions %}
\needspace{6\baselineskip}
{\large\bfseries Вариант~{{ sol.variant_num }}}

\begin{enumerate}[leftmargin=*, label=\textbf{\arabic*.}, itemsep=10pt]
{% for item in sol.items %}
    \item
    \textbf{Уравнение:}
    \[
        {{ item.condition_latex }}
    \]
    \textbf{Ответ:}
    \[
        {{ item.answer_latex }}
    \]
{% endfor %}
\end{enumerate}

{% if not loop.last %}
\bigskip\hrule\bigskip
{% endif %}
{% endfor %}

\end{document}
